'use strict';
var assert = require('assert');
var ibmConnectionsWikisService = require('../');

describe('ibm-connections-wikis-service node module', function () {
  it('must have at least one test', function () {
    ibmConnectionsWikisService();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
